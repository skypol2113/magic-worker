# MagicAIbox — Phase 1 Roadmap (God-Mode MVP + Donate)

## 0. Цель фазы

Собрать и выкатить в стор (Google Play / App Store) стабильный **MVP в “режиме бога”**:
- Все Pro-функции включены (ассистент, переводы, семантический матчинг).
- Нагрузка контролируется **лимитом интентов и rate‑limit’ами**, чтобы не уронить сервис и не взорвать бюджет.
- Добавлен **донат** как мягкая монетизация и способ поддержки проекта.
- Локализация и push‑уведомления корректно работают на **8 языках**.
- Включены анти‑спам/анти‑дубликаты/anti‑abuse механизмы.

После завершения Phase 1 можно публиковать приложение в сторе и собирать живую обратную связь.

---

## 1. App (Flutter клиент)

### 1.1. Донат

**Цель:** дать пользователям возможность поддержать проект в “god‑mode” фазе, без жёсткого paywall.

- **Задачи:**
  - [ ] Добавить кнопку **«💫 Поддержать MagicAIbox»** в удобном и видимом месте:
    - Вариант A (рекомендуемый): в экране **About / Settings / О проекте** (открывается из профиля или из главного меню).
    - Вариант B: внизу **ProfileScreen**, но при этом убедиться, что профиль доступен не только на первом запуске, а через нижнюю навигацию.
  - [ ] Открывать по нажатию:
    - либо in‑app экран с вариантами доната,
    - либо внешнюю ссылку (Buy Me a Coffee / Boosty / Ko‑fi / Stripe Checkout и т.п.).
  - [ ] После успешного доната показать экран благодарности (Thank‑you screen) и при желании:
    - временный бонус (например, +N интентов или “Pro на неделю”).
- **Статус:** TODO (дизайн + реализация).

### 1.2. Ограничение числа интентов (god‑mode, но с лимитами)

**Цель:** не уронить сервис и не перегрузить Vertex/OpenAI.

- **Задачи:**
  - [ ] На клиенте показывать пользователю, сколько активных интентов уже опубликовано (progress‑бар или счетчик).
  - [ ] При достижении лимита (например, 10 активных интентов):
    - блокировать кнопку «Опубликовать»,
    - показывать понятное сообщение (“Вы достигли лимита активных желаний. Сначала завершите или скройте часть из них.”).
  - [ ] Добавить soft‑cooldown на публикацию (например, не чаще 1 раза в 60 секунд).
- **Статус:** Часть логики есть на backend (проверки можно добавить), UI‑подсказки и жёсткий лимит — TODO.

### 1.3. Перевод интента оппонента + оригинал в MatchDetail

**Цель:** удобная работа с мультиязычными матчами без ручного копирования текста.

- **Задачи:**
  - [ ] В `MatchDetailScreen` добавить UI:
    - текст матча по умолчанию на языке приложения (если есть перевод),
    - тумблер/кнопку «Показать оригинал» (для Pro‑режима),
    - для Lite — иконку замка и подсказку “Доступно в Pro” (на будущее).
  - [ ] При первом открытии матча:
    - если `translations.<targetLang>` пусто → вызвать `/api/deliver`,
    - пока идёт запрос — показать skeleton/loader для текста.
  - [ ] Добавить иконку “copy” для копирования текста матча (перевод или оригинал).
- **Статус:** Базовый UI есть, полноценный перевод + переключение оригинал/перевод — TODO.

### 1.4. Локализация (8 языков, включая push‑сообщения)

**Цель:** чтобы пользователь на любом языке получил цельный опыт — от интерфейса до push‑уведомлений.

- **Задачи:**
  - [ ] Обновить ARB‑файлы (`lib/l10n/*.arb`):
    - добавить строки для доната,
    - тексты для push‑уведомлений,
    - подсказки ассистента, лимитов, ошибок.
  - [ ] Проверить корректность для CJK и RTL (zh, ja, ar):
    - переносы, направления текста, иконки.
  - [ ] Обновить `MaterialApp`/`LocalizationDelegates` при необходимости.
- **Статус:** Основная локализация есть, новые строки под Phase 1 — TODO.

---

## 2. Worker / Backend (Node.js + Firestore)

### 2.1. God‑mode Pro: все функции включены

**Цель:** на время MVP все пользователи получают опыт “как в Pro”, но with safety rails.

- **Задачи:**
  - [ ] Ввести конфиг/переменную окружения `APP_MODE=god` или флаг наподобие `ASSIST_FORCE_ENABLED=true`.
  - [ ] Игнорировать реальные планы (Lite/Pro) на backend в Phase 1 для функций:
    - ассистент,
    - переводы,
    - семантический матчинг.
  - [ ] При этом сохранить все лимиты (по интентам, по частоте запросов и т.д.).
- **Статус:** Ассист/embeddings/translate уже работают, режим “всем всё разрешено” по сути включен, нужно оформить флагами.

### 2.2. Лимиты интентов и anti‑abuse

**Цель:** контролировать нагрузку и защищать сервис.

- **Задачи:**
  - [ ] При публикации интента считать активные intents пользователя (`status='published'`).
  - [ ] При превышении лимита (например, 10) — возвращать ошибку с понятным кодом (`too_many_intents`).
  - [ ] Реализовать soft rate‑limit на:
    - публикацию интентов,
    - запросы к ассистенту (уже частично есть),
    - запросы на переводы.
  - [ ] Убедиться, что анти‑дубликаты / idempotency включены:
    - генерация pairKey для матчей,
    - проверка существующего матча перед созданием нового.
- **Статус:** Часть логики реализована, требуется финальный проход и тесты.

### 2.3. Ассистент: улучшение предложений и фасетов

**Цель:** чтобы ассистент не просто “переформулировал текст”, а **добавлял смысловые фасеты**, описывающие желание.

- **Задачи:**
  - [ ] Обновить system prompt для `/api/assist/continue`, чтобы модель:
    - понимала, что это “желание” / “offer” в контексте MagicAIbox,
    - возвращала не только текст, но и список фасетов/тэгов (обучение, услуги, обмен, музыка и т.п.).
  - [ ] Оформить формат ответа ассистента как JSON:
    - `[{ "text": "...", "facets": ["...", "..."] }, ...]`,
    - fallback: если модель вернула только массив строк, facets = `[]`.
  - [ ] Сохранять facets в поле `ai.facets` в документе интента.
  - [ ] При матчинге учитывать facets для категорий/фильтрации (например, `товары/услуги/знакомства/обучение`).
- **Статус:** Ассистент уже работает, но фасеты пока либо stub, либо отсутствуют — TODO улучшение промпта + парсера.

### 2.4. Переводы (intents + matches + push‑тексты)

**Цель:** единая история переводов:

- **Задачи:**
  - [ ] Убедиться, что модуль `translators` использует GCT v3/Vertex с ADC.
  - [ ] Обновить `/api/deliver`:
    - реальный перевод вместо stubs,
    - кэширование перевода в `matches.translations.{lang}`.
  - [ ] Локализовать push‑уведомления (new_match, message, contact_request):
    - шаблонные тексты на 8 языках,
    - выбор языка получателя по `users_private.preferences.interfaceLanguage` или fallback EN.
- **Статус:** Базовые переводы и deliver реализованы в упрощённом виде; мультиязычный push и полноценные кэши — TODO.

### 2.5. ADC (Application Default Credentials)

**Цель:** убрать секреты из `.env` и использовать нативную аутентификацию GCP.

- **Задачи:**
  - [ ] Удалить из `.env` `project_id`, `client_email`, `private_key` и т.п.
  - [ ] Инициализировать Firebase Admin SDK и GCP клиенты **только через ADC**.
  - [ ] Проверить работу в средах:
    - локально (через `gcloud auth application-default login`),
    - Cloud Run / Functions / Workstations (ADC по умолчанию).
- **Статус:** Частично сделано, требуется доочистка и тесты инициализации.

---

## 3. Локализация и Push‑уведомления (end‑to‑end)

### 3.1. App + Worker согласованность языков

**Цель:** чтобы язык интерфейса, язык push’ей и язык текстов в матчах были согласованы.

- **Задачи:**
  - [ ] На клиенте хранить и обновлять `interfaceLanguage` в профиле пользователя.
  - [ ] В воркере использовать это поле как основной таргет для:
    - переводов матчей,
    - перевода push‑текстов.
  - [ ] Добавить fallback стратегию:
    - `userLang` → если нет перевода → `en` → оригинал.
- **Статус:** Основа есть, единая стратегия и реализация push‑локализации — TODO.

### 3.2. Тестовый чек‑лист для 8 языков

**Цель:** не выпустить в стор полусырой мультиязычный клиент.

- **Чек‑лист:**
  - [ ] Авторизация, Welcome, HomeScreen.
  - [ ] Создание и публикация желания.
  - [ ] Получение матча + открытие `MatchDetailScreen`.
  - [ ] Просмотр перевода и оригинала текста.
  - [ ] Получение push‑уведомления и переход в нужный экран.
  - [ ] Отображение кнопки доната и экрана “О проекте / Donate”.

---

## 4. Инфраструктура и эксплуатация

### 4.1. Настройка окружения для Phase 1

- **Задачи:**
  - [ ] Cloud Run / VM с воркером:
    - `min_instances = 0` (экономия),
    - разумный `concurrency` (например, 5–10),
    - выставленные таймауты для ассиста и embeddings.
  - [ ] Логи и алерты:
    - базовые метрики в Cloud Logging/Monitoring,
    - алерт на резкий всплеск ошибок (5xx, таймауты, ошибки GCT/OpenAI).
- **Статус:** Основная инфраструктура есть, алерты + финишная настройка — TODO.

### 4.2. Что НЕ делаем в Phase 1 (но помним)

Эти пункты — для следующей фазы (Phase 2: Refactor & Scale):

- Разделение на HTTP‑воркер и Firestore‑слушателя (Cloud Functions 2‑го поколения / Eventarc).
- Включение полноценной Lite/Pro тарифной модели с оплатой.
- Реализация Lite языконезависимого matcher’а с упрощённой логикой (джаккард/косинус по токенам).
- Глубокая аналитика (time‑to‑first‑match, retention, конверсия в донаты/Pro).

---

## 5. Релиз‑чек‑лист (готовность к публикации в стор)

1. ✅ App собирается в release‑режиме, без dev‑флагов и отладочных баннеров.
2. ✅ Все секреты удалены из кода и `.env`, используется ADC.
3. ✅ God‑mode Pro включен, но:
   - стоит лимит на количество активных интентов,
   - работают анти‑дубликаты и rate‑лимиты.
4. ✅ Донат‑кнопка и экран “О проекте / Donate” работают и локализованы.
5. ✅ Ассистент выдаёт адекватные подсказки + фасеты (без явных глупостей).
6. ✅ Переводы интентов и матчей работают, оригинал можно посмотреть.
7. ✅ Push‑уведомления приходят и корректно открывают нужные экраны.
8. ✅ Базовый мониторинг включен, в логах нет лавины ошибок.
9. ✅ Минимальный набор тестов (ручных/автоматических) пройден для всех 8 языков.

После выполнения этого чек‑листа Phase 1 можно считать завершённой и двигаться к Phase 2 (Refactor & Scale).
