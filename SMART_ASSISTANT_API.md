# Smart Assistant API — Умные Вопросы для Лучшего Матчинга

## Обзор

Новая версия ассистента (v14) теперь возвращает **умные контекстные вопросы** (`smartQuestions`), которые помогают пользователям создать детальные интенты, достигающие 75%+ similarity для успешного матчинга.

## Эндпоинт

```
POST /api/assist/continue
```

## Запрос

```json
{
  "text": "нужна помощь с покупками",
  "lang": "ru",
  "uid": "user_id_here"
}
```

## Ответ (новая структура)

```json
{
  "ok": true,
  "items": [
    {
      "text": "Нужна помощь с покупками и немного общения",
      "category": "service",
      "direction": "seek",
      "facets": ["помощь", "покупки", "общение"],
      "attributes": {
        "type": "shopping_assistance",
        "frequency": null,
        "budget": null
      },
      "missingFields": ["location", "schedule"],
      "missingFieldsLabels": ["Место?", "Расписание?"],
      "recommendedFields": ["frequency", "budget"],
      "recommendedFieldsLabels": ["Как часто?", "Бюджет?"],
      "smartQuestions": [
        {
          "field": "location",
          "question": "В каком районе вам нужна помощь?",
          "why": "Найдём помощника рядом с вами"
        },
        {
          "field": "schedule",
          "question": "Как часто нужна помощь?",
          "why": "Поможет найти того, кто свободен в это время"
        },
        {
          "field": "details",
          "question": "Что именно нужно покупать?",
          "why": "Уточним объём и характер помощи"
        }
      ]
    }
  ],
  "cached": false,
  "ms": 2156,
  "godMode": false
}
```

## Новое поле: `smartQuestions`

Массив из 2-4 умных вопросов, отсортированных по важности для матчинга.

### Структура каждого вопроса:

```typescript
{
  field: string;      // Техническое имя поля (для связи с формой)
  question: string;   // Естественный вопрос для пользователя
  why: string;        // Объяснение, зачем нужен ответ (ценность для матчинга)
}
```

### Примеры `smartQuestions`:

**Для market (продажа):**
```json
[
  {
    "field": "price",
    "question": "Какая цена?",
    "why": "Покупатели ищут в своём бюджете"
  },
  {
    "field": "location",
    "question": "Где находится?",
    "why": "Найдём покупателей поблизости"
  },
  {
    "field": "brand",
    "question": "Какой бренд?",
    "why": "Кто-то ищет конкретную марку"
  }
]
```

**Для learning (обучение):**
```json
[
  {
    "field": "subject",
    "question": "Какой предмет хотите изучать?",
    "why": "Найдём подходящего преподавателя"
  },
  {
    "field": "level",
    "question": "Какой у вас уровень?",
    "why": "Поможет подобрать программу обучения"
  },
  {
    "field": "schedule",
    "question": "Когда удобно заниматься?",
    "why": "Найдём того, кто свободен в это время"
  }
]
```

**Для rideshare (попутчики):**
```json
[
  {
    "field": "from",
    "question": "Откуда едете?",
    "why": "Найдём попутчиков на вашем маршруте"
  },
  {
    "field": "to",
    "question": "Куда направляетесь?",
    "why": "Совпадение маршрутов критично"
  },
  {
    "field": "departureTime",
    "question": "Когда планируете выехать?",
    "why": "Важно для координации времени"
  }
]
```

## UI/UX рекомендации

### 1. Отображение вопросов

```dart
// Flutter пример
ListView.builder(
  itemCount: item.smartQuestions.length,
  itemBuilder: (context, index) {
    final q = item.smartQuestions[index];
    return Card(
      child: ListTile(
        leading: Icon(Icons.help_outline, color: Colors.purple),
        title: Text(q.question),
        subtitle: Text(q.why, style: TextStyle(fontSize: 12, color: Colors.grey)),
        onTap: () => _handleQuestionTap(q),
      ),
    );
  },
)
```

### 2. Интерактивные ответы

- **Тап на вопрос** → открыть поле ввода для ответа
- **Показывать прогресс** → "3 из 4 вопросов отвечены"
- **Визуальная обратная связь** → галочка при заполнении поля
- **Не блокировать** → пользователь может пропустить вопросы

### 3. Магический UX

```dart
// Показывать влияние на matching
Text('Ваш интент готов на ${completeness}% для матчинга')

// Анимация при ответе на вопрос
AnimatedBuilder(...)

// Поощрение
if (answeredQuestions >= 3) {
  showSnackbar('✨ Отлично! Теперь шансы на матч выше 75%!');
}
```

### 4. Адаптивность

- **1-2 вопроса** → Показать inline под текстом
- **3-4 вопроса** → Карточки или expansion tiles
- **Мобильный** → Swipe cards для навигации по вопросам
- **Десктоп** → Боковая панель с вопросами

## Логика работы

### Как ассистент выбирает вопросы:

1. **Анализ категории и направления** — понимает контекст (market/sell, learning/teach и т.д.)
2. **Определение критичных полей** — какие поля обязательны для матчинга в этой категории
3. **Приоритизация** — сортирует по влиянию на similarity score
4. **Генерация естественных вопросов** — не "Введите цену", а "Какая цена?"
5. **Добавление объяснения** — почему этот вопрос важен для пользователя

### Как это повышает similarity:

- **Детальные интенты** → больше семантической информации
- **Структурированные данные** → лучше сравниваются эмбеддинги
- **Контекстная релевантность** → интенты с заполненными ключевыми полями естественно достигают 75%+

## Миграция с предыдущей версии

Если у вас уже есть UI для `missingFields` / `recommendedFields`:

1. **Добавьте обработку `smartQuestions`** — новое поле, старые поля остаются
2. **Опциональная поддержка** — если `smartQuestions` пусто, используйте `missingFieldsLabels`
3. **Постепенный переход** — можно показывать оба интерфейса и A/B тестировать

```dart
if (item.smartQuestions.isNotEmpty) {
  return SmartQuestionsWidget(questions: item.smartQuestions);
} else {
  return LegacyFieldsWidget(fields: item.missingFieldsLabels);
}
```

## Тестирование

### Примеры интентов для тестирования:

```bash
# Market
curl -X POST http://localhost:3000/api/assist/continue \
  -H "Content-Type: application/json" \
  -d '{"text":"продам велосипед","lang":"ru","uid":"test"}'

# Service
curl -X POST http://localhost:3000/api/assist/continue \
  -H "Content-Type: application/json" \
  -d '{"text":"нужна помощь с уборкой","lang":"ru","uid":"test"}'

# Learning
curl -X POST http://localhost:3000/api/assist/continue \
  -H "Content-Type: application/json" \
  -d '{"text":"хочу учить английский","lang":"ru","uid":"test"}'
```

## Кэширование

Кэш версии: **v14** (автоматически сбрасывается при обновлении)

TTL: 60 секунд

Ключ кэша включает: `lang`, `text`, `ASSIST_FACETS_MAX`, `ASSIST_MISSING_MAX`, `ASSIST_RECOMMENDED_MAX`

## Конфигурация

```env
# .env
ASSIST_ENABLED=true
ASSIST_MODEL=gpt-4o-mini
ASSIST_MAX_TOKENS=800
ASSIST_FACETS_MAX=8
ASSIST_MISSING_MAX=4
ASSIST_RECOMMENDED_MAX=3
```

## Будущие улучшения

- [ ] Персонализация вопросов на основе истории пользователя
- [ ] Адаптивные вопросы (следующий вопрос зависит от предыдущего ответа)
- [ ] Автозаполнение из профиля (location, languages и т.д.)
- [ ] Предложения на основе похожих успешных интентов
- [ ] Мультиязычные вопросы с автоопределением языка

## Поддержка

Если у вас вопросы или идеи по улучшению ассистента — открывайте issue или пишите в Telegram!

---

**Магия в деталях! 🪄✨**
